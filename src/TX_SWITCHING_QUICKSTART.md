# 🚀 Быстрый старт: Переключение между TX

## ⚠️ ОБЯЗАТЕЛЬНАЯ НАСТРОЙКА

Перед использованием **НЕОБХОДИМО** заменить UID в коде!

### Шаг 1: Получите UID каждого TX

1. Включите TX в режиме WiFi
2. Подключитесь к веб-интерфейсу (обычно `http://10.0.0.1`)
3. Найдите раздел с информацией о bind phrase / UID
4. Запишите **6 байт UID** для каждого TX

**Пример UID:** `00 00 11 22 33 44`

### Шаг 2: Откройте файл rx_main.cpp

Найдите строки примерно на **строке 105-110**:

```cpp
const uint8_t TX_UIDS[MAX_TX_COUNT][UID_LEN] = {
    {0x00, 0x00, 0x11, 0x22, 0x33, 0x44},  // TX1 - ЗАМЕНИТЕ!
    {0x00, 0x00, 0x55, 0x66, 0x77, 0x88},  // TX2 - ЗАМЕНИТЕ!
    {0x00, 0x00, 0x99, 0xAA, 0xBB, 0xCC}   // TX3 - ЗАМЕНИТЕ!
};
```

### Шаг 3: Замените на ваши UID

Пример с реальными UID:

```cpp
const uint8_t TX_UIDS[MAX_TX_COUNT][UID_LEN] = {
    {0x00, 0x00, 0x12, 0x34, 0x56, 0x78},  // TX1 - RadioMaster
    {0x00, 0x00, 0xAB, 0xCD, 0xEF, 0x12},  // TX2 - Jumper
    {0x00, 0x00, 0x98, 0x76, 0x54, 0x32}   // TX3 - BetaFPV
};
```

### Шаг 4: Настройте интервал переключения (опционально)

По умолчанию: **5 секунд**

Чтобы изменить, найдите строку:
```cpp
uint32_t txSwitchInterval = 5000;  // в миллисекундах
```

Примеры:
- `3000` = 3 секунды (быстрое переключение)
- `10000` = 10 секунд (медленное переключение)
- `15000` = 15 секунд (очень медленное)

### Шаг 5: Скомпилируйте и прошейте RX

```bash
pio run -t upload
```

### Шаг 6: Проверка работы

1. Включите RX с OLED дисплеем
2. Включите все TX (или по одному)
3. На дисплее вы увидите:

```
Connected to TX1
TX1: -75 dBm
TX2: -68 dBm
TX3: ---
LQ:100 SNR:8
```

RX будет автоматически переключаться между TX каждые 5 секунд!

## 📊 Что отображается на дисплее

- **TX1/TX2/TX3:** RSSI каждого передатчика в dBm
- **"---":** TX еще не был активен или выключен
- **Connected to TXn:** К какому TX сейчас подключен RX
- **LQ:** Link Quality (качество связи)
- **SNR:** Signal-to-Noise Ratio (отношение сигнал/шум)

## 🔧 Дополнительная настройка

### Изменение количества TX

В файле `rx_main.cpp`:
```cpp
#define MAX_TX_COUNT 3  // Измените на 2, 4, 5 и т.д.
```

⚠️ Не забудьте добавить/удалить соответствующие строки в массиве `TX_UIDS`!

### Debug логи

При включенном DEBUG_LOG в Serial Monitor вы увидите:
```
Switched to TX1, UID=(0,0,18,52,86,120)
lost conn fc=0 fo=0
got conn
Switched to TX2, UID=(0,0,171,205,239,18)
```

## ❓ FAQ

**Q: Можно ли использовать одинаковые bind фразы?**  
A: НЕТ! Каждый TX должен иметь **уникальный UID**.

**Q: Что если один TX выключен?**  
A: RX попытается к нему подключиться, не найдет, и через timeout перейдет к следующему.

**Q: Можно ли переключаться вручную?**  
A: Да, но потребуется добавить кнопку и код. См. документацию.

**Q: Сколько времени занимает переключение?**  
A: ~5 секунд (интервал) + 1-2 секунды (синхронизация) = 6-7 секунд на переключение.

## 📖 Полная документация

См. файл [MULTI_TX_SWITCHING.md](MULTI_TX_SWITCHING.md) для подробной информации.
