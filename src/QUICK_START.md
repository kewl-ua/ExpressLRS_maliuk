# КРАТКОЕ РУКОВОДСТВО: Кастомные данные на каналах 10-15

## 📋 Что было сделано

Реализована система отправки текстовых данных на каналы 10-15 ExpressLRS с интервалом 5 секунд:

```
Канал 10 → "MY"
Канал 11 → "NAME"
Канал 12 → "IS"
Канал 13 → "ARTEM"
Канал 14 → "I"
Канал 15 → "AM"
Канал 16 → "UKRAINIAN"
```

## 🔧 Интегрированные компоненты

### TX (Передатчик) - уже готово

✅ Файлы созданы:
- `include/CustomChannelData.h` - класс для кодирования
- `src/CustomChannelData.cpp` - реализация
- Изменения в `src/tx_main.cpp` - интеграция

✅ Как это работает:
1. При запуске вызывается `CustomChannelData::init()`
2. В каждом loop() проверяется интервал 5 секунд
3. Текст кодируется в CRSF значения (0-2047)
4. Значения отправляются на каналах 10-15 через OTA пакет

### RX (Приемник) - опционально

Пример реализации в `RX_CUSTOM_CHANNELS_EXAMPLE.cpp`:
- Функция для декодирования значений обратно
- Обработчик изменений на каналах
- Примеры интеграции в RX loop

## 📊 Алгоритм кодирования текста

```
1. Вычисляется среднее ASCII значение символов
2. Применяется формула масштабирования
3. Результат: 11-битное значение CRSF (0-2047)
```

Пример кодирования:
- "MY" → ~824
- "NAME" → ~965  
- "ARTEM" → ~1190

## 🚀 Как скомпилировать и использовать

### Компиляция TX

```bash
# В директории проекта
pio run -e [board]  # Вместо [board] подставить вашу плату

# Или для конкретной платы, например:
pio run -e esp32
```

### На стороне RX

Если хотите обработать эти данные на приемнике:

1. Скопируйте функции из `RX_CUSTOM_CHANNELS_EXAMPLE.cpp` в `rx_main.cpp`
2. Вызывайте `handleCustomChannelData()` в loop()
3. Данные будут доступны через `ChannelData[10-15]`

## 💾 Файловая структура

```
include/
  └─ CustomChannelData.h              ← Заголовок модуля
src/
  ├─ CustomChannelData.cpp            ← Реализация
  ├─ tx_main.cpp                      ← Интегрировано ✓
  └─ RX_CUSTOM_CHANNELS_EXAMPLE.cpp   ← Пример для RX
CUSTOM_CHANNELS_README.md             ← Полная документация
IMPLEMENTATION_SUMMARY.md             ← Техническая сводка
QUICK_START.md                        ← Это файл
```

## ⚙️ Настройка (опционально)

Если нужно изменить параметры, отредактируйте `include/CustomChannelData.h`:

```cpp
// Интервал отправки (в миллисекундах)
static constexpr uint32_t INTERVAL_MS = 5000;  // Измените на нужное значение

// Стартовый канал
static constexpr uint8_t START_CHANNEL = 10;   // Измените если нужно другой канал
```

И отредактируйте текст в `src/CustomChannelData.cpp` в функции `applyToChannels()`:

```cpp
const char* textData[] = {
    "MY",           // ← Измените эти строки на свои данные
    "NAME",
    // ...
};
```

## 📡 Как работает радиосвязь

```
TX модуль                           RX модуль
    ↓                                   ↓
CustomChannelData кодирует      ChannelData распаковывает
текст в значения                 значения в каналы
    ↓                                   ↓
ChannelData[10-15] заполнены    Доступны через ChannelData[10-15]
    ↓                                   ↓
OTA пакет отправляется         OTA пакет распаковывается
    ↓                                   ↓
Передача по радио ────────────────────→
```

## 🔍 Отладка

Для отладки смотрите логи через serial консоль:

```
[CustomChannelData инициализирован - отправка текста на каналы 10-15...]
[CustomChannelData: отправка данных (время=5000)]
[CH10='MY' -> value=824]
[CH11='NAME' -> value=965]
...
```

## ✅ Проверка работы

На TX модуле (через консоль/логи):
- ✓ Видны сообщения инициализации
- ✓ Каждые 5 секунд появляются новые значения
- ✓ Значения в диапазоне 0-2047

На RX модуле (если реализована обработка):
- ✓ Получены значения на каналах 10-15
- ✓ Значения изменяются каждые 5 секунд
- ✓ Значения соответствуют TX стороне

## 📞 Поддержка

Ошибки компиляции? Проверьте:
1. ✓ `CustomChannelData.h` в папке `include/`
2. ✓ `CustomChannelData.cpp` в папке `src/`
3. ✓ Включение `#include "CustomChannelData.h"` в `tx_main.cpp`
4. ✓ Вызов `CustomChannelData::init()` в setup()
5. ✓ Вызов `CustomChannelData::update()` в loop()

## 📝 Примечания

- ✓ Механизм полностью неинвазивен
- ✓ Не влияет на стандартные каналы 0-9
- ✓ Каналы 10-15 теперь зарезервированы
- ✓ Система готова к расширению (добавлению новых типов данных)
- ✓ Кодирование детерминировано и обратимо

## 🎯 Следующие шаги

1. **Компилируйте и загружайте** на TX модуль
2. **Проверьте логи** - должны видны сообщения инициализации
3. **Опционально**: Реализуйте обработку на RX (см. пример)
4. **Используйте** получаемые данные в вашей логике

---

**Реализовано на русском языке для удобства использования.**

**Все файлы готовы к компиляции и использованию!**
